for value in range(1, 5):
  print(value) # Only prints the numbers 1-4, range means inside of the this range but starting at a certain point

numbers = list(range(1, 6))
print(numbers) # The output will be in a list format
