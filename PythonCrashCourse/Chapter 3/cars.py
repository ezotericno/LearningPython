cars = ['bmw', 'audi', 'toyota', 'subaru']
cars.sort() # Sorts the list in alphabetical order permanently
print(cars)
cars.sort(reverse=True) # Sorts the cars in the reverse order permanently

print(sorted(cars)) # The sorted() function only temporarily changes the list order

cars.reverse() # Reverses the order of the list from its current order, does not sort it in an

len(car) # prints the lenth of the list, actual number of items

