cars = ['audi', 'bmw', 'subaru', 'toyota']

for car in cars:
  if car == 'bmw':
    print(car.upper())
  else:
    print(car.title())

# Notice the doouble = sign, a single = sign means assignment, a double is an equality checker
# equality checks are case sensitive, so don't forget to capitalize things and debug
